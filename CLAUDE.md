# ADS — C++ 코딩 규칙

이 리포지토리는 **C++17 · ROS 2 Humble** 스택이다.
전역 가이드라인(`~/.claude/CLAUDE.md`)은 C 기준이므로, 이 문서가 이 리포에서 우선한다.
전역 규칙 중 이식 가능한 것은 그대로 유지하고, C++ 와 충돌하는 항목만 아래에서 명시적으로 대체한다.

---

## 0. 단 하나의 규칙

> **알고리즘은 `ad_core` 에, ROS 는 노드에.**

`ad_core` 는 `rclcpp` 를 include 하지 않는다. 이 규칙을 깨면 세 가지를 동시에 잃는다.

- 시뮬레이터 · GPU · roscore 없이 초 단위로 도는 단위 테스트
- Humble → Jazzy 이관 시 `ad_core` 무수정
- ROS 가 없는 양산 ECU 로의 이식성

되돌리는 비용은 처음 지키는 비용의 열 배다.

---

## 1. 전역 규칙에서 그대로 유지하는 것

| 항목 | 규칙 |
|---|---|
| 중괄호 | `if` · `else` · `switch` · `while` · `do` · `for` 는 단문·공문이라도 `{ }` 로 감싼다. 여는 중괄호는 다음 줄에 둔다 |
| 들여쓰기 | 4칸. 탭 문자 금지. 한 줄 150자 이하 |
| 공백 | 이항 연산자 앞뒤 한 칸, 단항 연산자는 피연산자 쪽에 공백 없음, `.` `->` 앞뒤 공백 없음, `[ ]` `( )` 안쪽 공백 없음 |
| 주석 | 완결된 문장으로 쓴다. 알고리즘 단계 블록 앞에 빈 줄 + 같은 들여쓰기 수준으로 둔다. 한 줄 코드의 의미가 불분명할 때만 쓴다 |
| 주석 TAG | `WARNING:` `NOTE:` `TODO:` `FIXME:` |
| 출처 | 공개 자료에서 온 알고리즘은 출처와 참조를 주석에 남긴다 |
| 함수 길이 | 300줄 이하 |
| 순환 복잡도 | 15 이하 |
| 파일 끝 | 마지막 줄에 빈 줄 |
| 헤더 가드 | 모든 헤더에 중복 포함 방지 |
| goto | 사용 금지 |

`.clang-format` 이 위 서식 규칙 대부분을 강제한다. 커밋 전에 `TOOLS/format.sh` 를 돌린다.

---

## 2. C++ 로 대체하는 것

### 2.1 명명

전역 C 규칙의 헝가리안 접두어(`u16Vanz_10En2_mps`)는 **적용하지 않는다.**
`auto` · 템플릿 · `std::optional` 앞에서 타입 접두어가 거짓말을 하기 시작하고,
STL 및 ROS2 인터페이스와 섞이면 일관성이 깨진다.

대신 다음을 쓴다.

| 대상 | 규칙 | 예 |
|---|---|---|
| 타입 · 클래스 · 열거형 | PascalCase | `StanleyController`, `VehicleState` |
| 인터페이스 | `I` 접두어 | `ILateralController` |
| 함수 · 메서드 | camelCase | `computeSteering()` |
| 지역 변수 · 매개변수 | snake_case | `lateral_error` |
| 멤버 변수 | snake_case + `_` 접미어 | `wheelbase_` |
| 상수 · `constexpr` | `k` + PascalCase | `kMaxSteerRad` |
| 매크로 | UPPER_SNAKE_CASE (거의 쓰지 않는다) | |
| 네임스페이스 · 파일명 | snake_case | `ad_core`, `stanley_controller.hpp` |
| ROS2 패키지 | snake_case (ament 요구사항) | `ad_sim_bicycle` |

### 2.2 단위 접미어는 유지한다

물리량은 **반드시 단위를 이름에 남긴다.** 이것이 전역 규칙에서 가장 값을 하는 부분이다.

```cpp
double speed_mps;          // m/s
double steer_rad;          // rad
double lateral_error_m;    // m
double yaw_rate_radps;     // rad/s
double dt_s;               // s
```

C 규칙의 정수 스케일 팩터 표기(`10En2`)는 부동소수점을 쓰는 이 스택에서는 불필요하다.
고정소수점을 쓰는 코드가 생기면 그때 `_10en2_mps` 형태로 되살린다.

### 2.3 전역 금지 항목의 완화

| 전역 규칙 | 이 리포 | 이유 |
|---|---|---|
| `auto` 금지 | **허용** — 반복자 · 템플릿 반환 · 구조적 바인딩에 한정 | C 의 `auto` 저장 클래스와 C++11 의 타입 추론은 다른 키워드다 |
| `continue` 금지 | **허용** — 조기 필터링으로 중첩을 줄일 때만 | 중첩 `if` 로 밀어내면 순환 복잡도가 오히려 오른다 |
| `break` 금지 (switch 외) | **허용** — 선형 탐색 종료에 한정 | |
| 단일 return 강제 | **완화** — 사전조건 검사의 조기 반환은 허용 | 가드 절이 있는 편이 읽기 쉽다 |
| `<stdlib.h>` 동적 할당 금지 | **RAII 로 대체** — `std::vector` · `std::unique_ptr` 사용, 생 `new`/`delete` 금지 | 실시간 경로에서는 여전히 할당을 피한다 (§3) |
| 입출력 함수 금지 | **`printf` 계열 금지 유지** — 로깅은 `RCLCPP_*` 매크로로 | |

### 2.4 인터페이스 설계

- 다형성이 필요한 곳은 순수 가상 인터페이스(`I` 접두어)로 계약을 먼저 고정한다
- 인터페이스에는 반드시 `virtual ~IFoo() = default;`
- 재정의에는 `override`, 확장 금지에는 `final`
- 값을 바꾸지 않는 메서드는 `const`, 예외를 던지지 않으면 `noexcept`
- 단일 인자 생성자에는 `explicit`

---

## 3. 실시간 경로에서 금지하는 것

`ad_control` 의 콜백과 `ad_core` 의 제어 계산 경로에서는 다음을 하지 않는다.

- 힙 할당 (`std::vector::push_back` 포함 — 사전에 `reserve` 한다)
- 예외 던지기
- 뮤텍스 블로킹 (락프리 링 버퍼를 쓴다 — VOL.4 4장)
- 파일 입출력 · 콘솔 출력
- `std::function` 을 통한 간접 호출 (지연 예측 불가)

---

## 4. 테스트

- 제어기를 만들 때 테스트를 **같은 커밋에** 만든다. 나중으로 미루지 않는다
- `ad_core` 테스트는 ROS 없이 돌아야 한다. 시뮬레이터를 요구하면 그 테스트는 잘못 놓인 것이다
- 테스트 이름은 동작을 문장으로 쓴다 — `SteersLeftWhenPathIsOnTheLeft`
- 부동소수점 비교에 `==` 를 쓰지 않는다. `EXPECT_NEAR` 와 명시적 허용오차를 쓴다

> NOTE: 이 스택을 설계할 때 `StanleyController` 의 횡편차 부호가 반대였다.
> 수식은 그럴듯했고 컴파일도 됐고 경고도 없었다. 위 이름의 테스트 하나가 즉시 잡아냈다.

---

## 5. MISRA C:2023

전역 규칙의 MISRA C 항목은 **C 코드에 적용된다.** 이 리포에는 C 코드가 없다.
양산 이식을 전제로 `ad_core` 를 C 로 내려쓰는 시점이 오면 그때 적용한다.
다만 다음 항목은 C++ 에서도 지킬 값이 있어 유지한다.

- 부호 없는 정수 상수에 `u` 접미어
- 8진 상수 금지 (`010` 은 8이 아니다)
- 재귀 금지 (스택 깊이 예측 불가)
- 모든 `switch` 에 `default` 라벨
- `if ... else if` 는 `else` 로 종결
- 연산자 우선순위를 괄호로 명시
